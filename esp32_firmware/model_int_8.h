#pragma once

// Compatibility shim for the older ArduTFLite sketch.
// The active generated model now lives in model.h as dbs_model/dbs_model_len.

#include "model.h"

#define model dbs_model
#define model_len dbs_model_len
